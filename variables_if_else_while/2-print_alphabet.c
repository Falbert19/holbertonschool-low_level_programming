#include <stdio.h>
/**
 * main - this will prnit the alphabet in lowercase
 * Return: always (Success)
 */

int main(void)
{
	char c:
	for (c = 'a'; c<= 'z'; c++)
	putchar(c)
	putchar('\n);
	return (0);
}
